export const mockJwtToken = 'random.super.secret.token';
export const mockRefreshedToken = 'new.random.secure.token';
export const mockPassword = 'StrongP4$$';

export const mockTenant = { id: 1, name: 'snapsoft.hu' };

export const mockUser = {
  id: 0,
  username: 'test.user',
  email: 'test.user@snapsoft.hu',
  tenants: [mockTenant.id],
};
